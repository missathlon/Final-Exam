import java.time.LocalDate;

public class Camel extends PackAnimal{
        public Camel (String name, LocalDate birth, String type, String command){
            super(name, birth, "Camel", command);
    }
}
